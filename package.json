{
  "name": "ndig",
  "version": "1.0.0",
  "description": "Get DNS records using dig",
  "main": "ndig.mjs",
  "scripts": {
    "pretest": "npm run lint",
    "test": "echo \"Error: no test specified\" && exit 1",
    "posttest": "npm run format",
    "format": "prettier --log-level warn --write \"**/*.{js,mjs,css,md}\"",
    "lint": "eslint . --cache --fix"
  },
  "keywords": [],
  "author": "Dan Revel <danrevel@gmail.com> (https://noco.ws/)",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "commander": "^14.0.3",
    "zx": "^8.8.5"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.8.3"
  },
  "bin": {
    "ndig": "./ndig.mjs"
  }
}
